TextCNN文本分类模型项目说明

模型结构设置

1) 核心架构选择
本项目采用TextCNN作为基础模型架构，特别适合处理文本分类任务
TextCNN优势：通过多尺度卷积核捕获不同长度的n-gram特征，训练稳定且收敛速度快
模型结构包含：
  嵌入层：将单词转换为密集向量表示
  多尺度卷积层：使用不同大小的卷积核提取局部特征
  全局池化层：将变长特征转换为固定长度表示
  全连接层：输出分类结果

2) 推荐结构参数
嵌入维度：128-200（本项目使用128，平衡性能与计算成本）
卷积核数量：100-256（本项目使用100，足够处理中等规模数据集）
卷积核大小：[3, 4, 5]（最佳实践，能有效捕获短文本中的关键特征）
Dropout率：0.2-0.5（本项目使用0.3，有效防止过拟合）

超参数选择指南

1) 关键超参数推荐值
学习率：0.005-0.01（SGD优化器）或 0.0001-0.001（Adam优化器）
批次大小：32-64（本项目使用32，适合大多数GPU配置）
训练轮次：10-20（配合早停机制，本项目使用20轮）
早停耐心值：3-5（本项目使用3，防止过拟合）

2) 超参数调优建议
优先调整：学习率 > 卷积核数量 > Dropout率
避免过度调整：卷积核大小组合[3,4,5]已被证明是最稳定有效的配置
内存考虑：若GPU内存有限，可适当降低batch_size或num_filters
数据集适配：对于短文本分类，filter_sizes=[2,3,4]可能更有效

优化方法选择

1) 推荐优化器
SGD + Momentum：optim.SGD(model.parameters(), lr=0.01, momentum=0.9)
  优势：训练稳定，不易陷入局部最优
  适用场景：大多数文本分类任务的首选优化器
Adam优化器：optim.Adam(model.parameters(), lr=0.001)
  优势：自适应学习率，收敛速度快
  注意：可能在训练后期不稳定，需配合学习率调度

2) 关键训练策略
梯度裁剪：torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
  必须使用：防止梯度爆炸，提高训练稳定性
学习率调度：建议添加ReduceLROnPlateau策略
  当验证损失不再下降时自动降低学习率
正则化技术：
  Dropout：已集成在模型中
  权重衰减：可考虑在优化器中添加weight_decay=1e-5

实践建议

模型初始化：使用Xavier或Kaiming初始化方法，避免梯度消失
数据预处理：
   必须去除停用词：显著提升模型性能
   序列长度：设置合理的最大长度（如100-200词）
性能监控：
   同时监控训练损失和验证准确率
   测试准确率与验证准确率差距应小于5%，否则可能存在过拟合

本项目配置已在20 Newsgroups数据集上验证，达到82.01%的测试准确率，符合TextCNN在二分类任务中的预期性能。如需进一步提升，建议优先调整学习率和卷积核数量，这些参数对性能影响最为显著。