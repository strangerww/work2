LSTM新闻分类模型项目说明
模型结构设置
核心架构选择
本项目采用双向长短期记忆网络（Bi-LSTM）作为基础模型架构，特别适合处理需要捕捉长距离依赖和上下文信息的文本分类任务。
LSTM优势：通过门控机制有效解决传统RNN的梯度消失问题，能够记忆长文本中的关键信息；双向结构能同时利用过去（前文）和未来（后文）的上下文特征，比单向模型理解更全面。
模型结构包含：
嵌入层：将单词索引转换为128维的密集向量表示。
双向LSTM层：2层堆叠结构，提取序列的时间序列特征。
Dropout层：在嵌入层和LSTM层之间及LSTM层间使用，防止过拟合。
全连接层：将双向隐藏状态拼接后映射到分类空间。
推荐结构参数
嵌入维度：128（本项目使用128，在词汇表大小14613的情况下，能较好平衡语义表达与计算资源）。
隐藏层维度：128（配合双向结构，实际输出维度为256）。
层数：2层（深层LSTM能提取更抽象的语义特征，但层数过多易导致训练困难）。
Dropout率：0.5（本项目使用0.5，较高的Dropout率对于防止LSTM在中小规模数据集上过拟合至关重要）。
超参数选择指南
关键超参数推荐值
学习率：0.001（Adam优化器）。Adam通常不需要像SGD那样精细调整学习率，0.001是默认且稳健的起始值。
批次大小：32（本项目使用32，适合大多数GPU显存配置，且能提供较稳定的梯度估计）。
训练轮次：20（配合早停机制，通常TextCNN/LSTM在10-20轮内即可收敛）。
早停耐心值：3（本项目使用3，一旦验证集准确率连续3轮不提升即停止，防止过拟合）。
超参数调优建议
优先调整：学习率 > Dropout率 > 隐藏层维度。
避免过度调整：批次大小通常设为32或64即可，对最终结果影响不如学习率显著。
序列长度：虽然使用了动态填充，但过长的文本会稀释关键信息，建议在预处理阶段限制最大长度（如500词）。
优化方法选择
推荐优化器
Adam优化器：optim.Adam(model.parameters(), lr=0.001)
优势：结合了动量和RMSprop的优点，自适应调整每个参数的学习率，收敛速度极快。
注意：在训练后期可能会有微小震荡，配合早停机制效果最佳。
关键训练策略
梯度裁剪：torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
必须使用：RNN/LSTM类模型极易出现梯度爆炸，裁剪梯度是保证训练稳定性的关键步骤。
早停机制：监控验证集准确率，保存最佳模型权重。
本项目已实现：当验证准确率不再提升时，自动停止训练并加载最佳模型。
正则化技术：
Dropout：已在模型中设置0.5的高比率Dropout。
权重衰减：若发现过拟合严重，可在Adam中添加weight_decay=1e-5。
实践建议
模型初始化：PyTorch默认初始化通常已足够，但对于LSTM，使用正交初始化（Orthogonal Initialization）处理权重矩阵通常能获得更好的收敛效果。
数据预处理：
去除停用词：本项目已去除 "the", "is" 等无意义词，这能显著降低噪声，提升模型对关键实体的关注度。
词汇表过滤：设置 min_freq=2，过滤掉极低频词，减少词汇表大小和模型参数量。
性能监控：
关注损失曲线：若训练损失持续下降而验证损失上升，说明过拟合（本项目已出现此迹象，但早停机制已生效）。
测试集表现：测试准确率与验证准确率差距应小于5%。
